name = "natalis-persianas"
compatibility_date = "2024-01-01"

# Roda antes do deploy (npx wrangler deploy)
[build]
command = "npm run build"

# Arquivos estáticos gerados em dist/
[assets]
directory = "./dist"
not_found_handling = "single-page-application"
