{
  "name": "natalis-persianas",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node scripts/build-static.mjs",
    "deploy": "npm run build && wrangler deploy"
  },
  "engines": {
    "node": ">=18"
  }
}
